import React, { Component } from 'react'
import styles from './dashboards.module.css'

export default class dashboards extends Component {
    render() {
        return (
            <div className={`${styles.test} d-flex justify-content-center`}>
                Dashboard
            </div>
        )
    }
}
